{
    "dev": {
        "files": [{
            "expand": true,
            "cwd": "<%= config.paths.source %>",
            "src": [
                "images/**/*.{png,jpg,gif,svg}"
            ],
            "dest": "<%= config.paths.build.dev_htdocs %>/_shared"
        }]
    }
}